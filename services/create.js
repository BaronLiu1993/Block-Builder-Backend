
//Chunk into Sections i
/**
    abstractedBlocks: [
        titleBlock: {
            coverTitle: 
            coverSubtitle:
            coverImage: //Link do not make it a blob at all
        },
        paragraphBlock: {
            paragraphText: 
            styling: []
        }
    ]   
 */

//Creates a new Database Entry and Then Creates an Abstracted Vercel Function
//That pushes to vercel adds to code base and then adds it properly

//Generate a HTML Tree That Fix
//Select Custom Fonts and Add

export async function generateHTMLTree() {
    for (const i = 0; i < abstractedBlocks.length; i++) {
        if (abstractedBlocks[i].type == "paragraph") {
            
        } else if (abstractedBlocks[i] == "paragraphBlock") {

        } else {
            
        }
    }
}